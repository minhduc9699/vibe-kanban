{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "required": [
    "provider"
  ],
  "description": "CCS (Claude Code Switch) executor - routes to multiple AI providers\nvia a unified Claude-compatible interface.\n\nProviders: gemini, codev, agy, qwen, iflow, kiro, ghcp",
  "type": "object",
  "properties": {
    "provider": {
      "description": "Provider to use (gemini, codev, agy, qwen, iflow, kiro, ghcp)",
      "type": "string"
    },
    "append_prompt": {
      "title": "Append Prompt",
      "description": "Extra text appended to the prompt",
      "type": [
        "string",
        "null"
      ],
      "format": "textarea",
      "default": null
    },
    "model": {
      "type": [
        "string",
        "null"
      ]
    },
    "dangerously_skip_permissions": {
      "type": [
        "boolean",
        "null"
      ]
    },
    "base_command_override": {
      "title": "Base Command Override",
      "description": "Override the base command with a custom command",
      "type": [
        "string",
        "null"
      ]
    },
    "additional_params": {
      "title": "Additional Parameters",
      "description": "Additional parameters to append to the base command",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "env": {
      "title": "Environment Variables",
      "description": "Environment variables to set when running the executor",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}